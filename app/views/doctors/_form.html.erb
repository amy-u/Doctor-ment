<%= form_for(doctor) do |f| %> <% if doctor.errors.any? %>
<div id="error_explanation">
  <h2>
    <%= pluralize(doctor.errors.count, "error" ) %> prohibited this doctor from
    being saved:
  </h2>

  <ul>
    <% doctor.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>
<form>
  <div class="form-group">
    <div class="col">
      <div class="field">
        <%= f.label :name %>
        <div class="text-lg-start"><%= f.text_field :name %></div>
      </div>
      <div class="field">
        <%= f.label :number %>
        <div class="text-lg-start"><%= f.text_field :number %></div>
      </div>
      <div class="field">
        <%= f.label :hospital_name %>
        <div class="text-lg-start"><%= f.text_field :hospital_name %></div>
      </div>
      <div class="field">
        <%= f.label :address %>
        <div class="text-lg-start"><%= f.text_field :address %></div>
      </div>
      <div class="field">
        <%= f.label :user_id %>
        <div class="text-lg-start"><%= f.text_field :user_id %></div>
      </div>
      <div class="action"><%= f.submit %></div>
    </div>
  </div>
</form>
<% end %>
